language: python
dist: trusty

python:
  - '3.6'
  - 'nightly'

sudo: false

install:
  - pip install --quiet --requirement requirements.txt

script:
  - python3 -m unittest --verbose

notifications:
  email:
    on_success: change
    on_failure: change
